import React, { FC } from "react";
import Head from "next/head";

export const MarketplaceListingContainer: FC = () => (
  <div className="flex flex-1 flex-col justify-center items-center h-screen">
    <Head>
      <title>GiveTree - Marketplace</title>
    </Head>

    <div>
      <h1 className="text-3xl sm:text-4xl font-bold">Marketplace coming soon...</h1>
    </div>
  </div>
);
